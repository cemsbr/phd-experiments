rsync:
	rsync -vrzcL --delete \
	    --exclude '.*.swp' \
	    --exclude '.git/' \
	    --exclude '.gitkeep' \
	    --exclude '__pycache__/' \
	    --exclude '*.log' \
	    remote_launcher nuvemusp:exp06/
